class RouterRenderer {

  /**
   * Renderer constructor.
   * 
   * @param {Node} view - Router state.
   * @param {Object}   infos - View DOM.
   * @constructor
   */
  constructor(view, infos) {
    // Debug.
    console.log('RENDERER');
  }
}

export default RouterRenderer;
